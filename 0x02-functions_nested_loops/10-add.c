#include "main.h"

/**
 * add - adds too integers together and returns the result
 * @a: takes the first number
 * @b: takes the second number
 * Return: Always 0 (Success)
*/
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
