#include "main.h"

/**
 * mul - a function that multiply two integers
 * @a: this the first integer
 * @b: this is the second integer
 * Return: Always 0 (Success)
*/

int mul(int a, int b)
{
	int multiply;

	multiply = a * b;
	return (multiply);
}
